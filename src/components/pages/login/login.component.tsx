// Dependencies
import React, { FunctionComponent } from "react";

// Layout
import { Layout } from "../../layouts";

export const Login: FunctionComponent = () => {
  return <Layout contentPage={<h1>Login</h1>} />;
};
