// Dependencies
import React, { FunctionComponent } from "react";

// Layout
import { Layout } from "../../layouts";

export const Home: FunctionComponent = () => {
  return <Layout contentPage={<h1>Home Page</h1>} />;
};
